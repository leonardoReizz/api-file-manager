import { test, expect } from "vitest";

test("get user controller ", () => {
  const a = "A";
  expect(a).toEqual("A");
});

test("get user controller ", () => {
  const a = "A";
  expect(a).toEqual("A");
});
